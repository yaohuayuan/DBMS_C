//
// Created by yaohuayuan on 2025/5/30.
//

#ifndef NEWDBMS_CCMATH_H
#define NEWDBMS_CCMATH_H
#include "math.h"
#define MIN(a,b) ((a) < (b) ? (a) : (b))
#define MAX(a,b) ((a) > (b) ? (a) : (b))
// 新增：整数比较函数

#endif //NEWDBMS_CCMATH_H
